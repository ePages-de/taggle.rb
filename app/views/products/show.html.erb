<span><%= @product.id %></span>
<span><%= @product.sku %></span>
<span><%= @product.name %></span>
<li>
  <% @product.tags.each do |tag| %>
    <ul><%= tag %></ul>
  <% end %>
</li>

<%= image_tag @product.image.href.split("{").first %>
